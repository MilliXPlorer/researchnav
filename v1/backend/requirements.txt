# FastText remains lazy-loaded so a missing model is a stable 503, not a startup leak.
scikit-learn>=1.5,<2.0
pypdf>=5.0,<7.0
python-docx>=1.1,<2.0
# fastText 0.9.3 is a source-only distribution written against pybind11 2.x.
# pybind11 3.x removes the py::init overloads and the global ssize_t typedef it
# needs, so keep this pin and install fastText with --no-build-isolation.
# See README.md for the Windows compiler flags this build also requires.
pybind11>=2.12,<3
fasttext>=0.9.2,<0.10
